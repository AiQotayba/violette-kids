# mobile app:

## v 1.1.3

notepad C:\Users\USER\AppData\Roaming\Microsoft\Windows\PowerShell\PSReadline\ConsoleHost_history.txt
cd && cd ~/violette-kids && git pull && cd apps/api/ && pnpm install && pnpm exec tsc && pm2 restart app

# admin panel:

- guidline colors for project
- rename app
